apiVersion: v1
kind: Service
metadata:
  name: mjh-backend
  namespace: mjh
  labels:
    app: mjh-backend
spec:
  type: LoadBalancer
  selector:
    app: mjh-backend
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
